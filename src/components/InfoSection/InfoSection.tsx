import React from "react";

type InfoSectionPropsType = {
    id?: string
}

export const InfoSection = (props: InfoSectionPropsType) => {
    return (
        <div>
            InfoSection
        </div>
    )
}